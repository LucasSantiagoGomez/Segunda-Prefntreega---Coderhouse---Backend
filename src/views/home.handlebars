<div id="app" >
  <h1>Lista de productos</h1>
  {{#each products}}
  <div>
    <br>
    <p>{{this.title}}</p>
    <p>{{this.description}}</p>
    <p>{{this.code}}</p>
    <p>{{this.price}}</p>
    <p>{{this.stock}}</p>
  </div>
  {{/each}}
</div>
